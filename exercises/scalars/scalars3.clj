;; scalars3.clj
;; Make me compile! Scroll down for hints :)

(ns scalars.scalars3)

(defn -main []
  (let [s (??? "Hello" "World")]
    (println s)))

































;; On line 7 we wish to concatenate two strings together for printing,
;; can you find a clojure function which does this?